echo "🔍 Running pre-commit checks..."

# Run full lint on client and server
echo "📝 Running linters..."
cd client && yarn lint && cd ../server && yarn lint && cd ..

# Run lint-staged for auto-fixing staged files
echo "🔧 Auto-fixing staged files..."
npx lint-staged

# Run server tests
echo "🧪 Running server tests..."
cd server && yarn test:e2e && cd ..

echo "✅ All pre-commit checks passed!"
